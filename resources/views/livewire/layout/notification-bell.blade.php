<flux:dropdown position="bottom" align="end">
    <flux:button icon="bell" variant="ghost" aria-label="Notificaciones"><span class="sr-only">Notificaciones</span>@if ($pendientes)<span class="absolute -right-1 -top-1 min-w-4 rounded-full bg-red-600 px-1 text-center text-[10px] text-white">{{ $pendientes }}</span>@endif</flux:button>
    <flux:menu class="w-80"><div class="flex items-center justify-between px-3 py-2"><flux:heading size="sm">Notificaciones</flux:heading><flux:button size="xs" variant="ghost" wire:click="markAllAsRead">Marcar leídas</flux:button></div>@forelse ($notificaciones as $notificacion)<flux:menu.item :href="$notificacion->tramite_id ? route('tramites.show', $notificacion->tramite_id) : '#'" wire:navigate>{{ $notificacion->titulo }}<span class="block text-xs text-zinc-500">{{ $notificacion->mensaje }}</span></flux:menu.item>@empty<flux:text class="px-3 py-4">No tienes notificaciones.</flux:text>@endforelse</flux:menu>
</flux:dropdown>